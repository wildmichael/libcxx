#- Version information file for LIBCXX

set(PACKAGE_VERSION @LIBCXX_VERSION@)

if(${PACKAGE_FIND_VERSION} VERSION_EQUAL ${PACKAGE_VERSION})
	set(PACKAGE_VERSION_EXACT TRUE)
else()
	set(PACKAGE_VERSION_UNSUITABLE FALSE)
endif()
